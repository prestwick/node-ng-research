/*global $, jQuery, document, io, angular*/

var apiServiceModule = angular.module('api-service', ['socket-service']);

apiServiceModule.factory('apiService', function ($rootScope, socket) {
    "use strict";
    socket.on
    return {}; //return exposed function
});